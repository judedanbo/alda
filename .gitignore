# Test artifacts
test-card.png
.playwright-mcp/

# Claude Code
.claude/

# Root-level lockfile (real one is in app/)
package-lock.json

# Stray root-level dependency/cache dir (real node_modules is in app/)
node_modules/
