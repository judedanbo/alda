apiVersion: v1
kind: ConfigMap
metadata:
  name: adla-config
data:
  # NUXT_-prefixed so it overrides runtimeConfig.public.appUrl at runtime
  # (plain APP_URL is ignored by a production Nuxt build — see base configmap).
  NUXT_PUBLIC_APP_URL: "https://alda.audit.gov.gh"
